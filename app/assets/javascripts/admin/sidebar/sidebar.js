angular.module('app')
  .controller('SidebarController', function ($scope, $location, $api, $window) {
    //add sidebar feature to show active page
    // $scope.current_location = $location.href();
  });
