<section class="container">
  <%= image_tag 'backgrounds/home-desk.svg', class: 'center-block' %>
</section>

<section class="container">
  <h2 class="text-center">SALT</h2>
  <h4 class="text-center">A crowdfunding platform that helps developers earn a monthly salary from open-source software.</h4>

  <div class="row">
    <div class="col-sm-3 welcome-card sign-up-card">
      <h5>Want your team on SALT?</h5>
      <hr>
      <p class="text-muted">Setup a campaign for your open-source project.</p>
      <a href="<%= ENV['BOUNTYSOURCE_WWW_URL'] %>teams/new?salt=true" class="btn btn-puerto-rico btn-rounded btn-lg btn-long mt-3">SIGN UP NOW</a>
    </div>
    <div class="col-sm-6 welcome-card activity-card">
      <h5>Recent Activity</h5>
      <hr>
      <div class="row">
        <div class="col-xs-4">
          <div style="font-size: 20px;">{{global_summary.current_month_sum | dollars}}</div>
          <div class="text-muted">paid this month</div>
        </div>

        <div class="col-xs-4">
          <div style="font-size: 20px;">{{global_summary.previous_month_sum | dollars}}</div>
          <div class="text-muted">paid last month</div>
        </div>

        <div class="col-xs-4">
          <div style="font-size: 20px;">{{global_summary.total_sum | dollars}}</div>
          <div class="text-muted">paid total</div>
        </div>
      </div>

      <div class="row mt-4">
        <div class="col-xs-4">
          <div style="font-size: 20px;">{{global_summary.current_month_cnt}}</div>
          <div class="text-muted">supporters</div>
        </div>

        <div class="col-xs-4">
          <div style="font-size: 20px;">{{global_summary.total_teams}}</div>
          <div class="text-muted">paid teams</div>
        </div>

        <div class="col-xs-4">
          <div style="font-size: 20px;">{{global_summary.salt_teams | number}}</div>
          <div class="text-muted">active teams</div>
        </div>
      </div>
    </div>

    <div class="col-sm-3 welcome-card why-salt-card">
      <h5>Why is it called SALT?</h5>
      <hr>
      <p class="text-muted pl-4 pr-4">The word salary comes from the Latin word salarium, or "salt-money,” a Roman soldier's allowance for the purchase of salt.</p>
    </div>
  </div>
</section>

<section class="container mt-6">
  <ul class="nav nav-tabs">
    <li role="presentation" ng-class="{ active: state.includes('root.home.teams.list') }"><a ui-sref="root.home.teams.list">Teams</a></li>
    <li role="presentation" ng-class="{ active: state.includes('root.home.updates') }"><a ui-sref="root.home.updates">Updates</a></li>
    <li role="presentation" ng-class="{ active: state.includes('root.home.activity') }"><a ui-sref="root.home.activity">Activity</a></li>
  </ul>
  <ui-view/>
</section>
